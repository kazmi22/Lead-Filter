# Copy this file to .env and fill in your values
OPENAI_API_KEY=sk-...
# Optional: override model
OPENAI_MODEL=gpt-4o
# Optional: allowed CORS origins (comma-separated). Use * for all.
ALLOWED_ORIGINS=*
